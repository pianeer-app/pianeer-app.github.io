name: check

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # No dependencies to install — tools/check.py is standard library only.
      - run: python3 tools/check.py
